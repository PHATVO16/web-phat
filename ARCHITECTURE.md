# 🏗️ KIẾN TRÚC HỆ THỐNG

## 📐 Tổng quan Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                        │
│                    (Bootstrap 5 + HTML5)                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │Dashboard │  │ Products │  │Categories│  │Employees │   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │
│       │             │              │              │          │
│  ┌────┴─────┐  ┌───┴──────┐      │              │          │
│  │  Orders  │  │Statistics│      │              │          │
│  └────┬─────┘  └────┬─────┘      │              │          │
│       │             │              │              │          │
└───────┼─────────────┼──────────────┼──────────────┼──────────┘
        │             │              │              │
        ▼             ▼              ▼              ▼
┌─────────────────────────────────────────────────────────────┐
│                    JAVASCRIPT LAYER                          │
│              (API Calls + Business Logic)                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  api.js  │  dashboard.js  │  sanpham.js  │  danhmuc.js     │
│  donhang.js  │  nhanvien.js  │  thongke.js                 │
│                                                              │
│  • AJAX Requests           • Data Formatting                │
│  • Error Handling          • UI Updates                     │
│  • Validation              • Event Listeners                │
│                                                              │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │ HTTP Requests (JSON)
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      REST API LAYER                          │
│                     (PHP Backend)                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ sanpham  │  │ danhmuc  │  │donvitinh │  │ nhanvien │   │
│  │   .php   │  │   .php   │  │   .php   │  │   .php   │   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │
│       │             │              │              │          │
│  ┌────┴─────┐  ┌───┴──────┐      │              │          │
│  │ donhang  │  │ thongke  │      │              │          │
│  │   .php   │  │   .php   │      │              │          │
│  └────┬─────┘  └────┬─────┘      │              │          │
│       │             │              │              │          │
│       ▼             ▼              ▼              ▼          │
│  ┌─────────────────────────────────────────────────┐       │
│  │         Database Configuration Layer             │       │
│  │            (database.php)                        │       │
│  │  • Connection Management                         │       │
│  │  • Helper Functions                              │       │
│  │  • Error Handling                                │       │
│  └─────────────────────┬───────────────────────────┘       │
│                        │                                     │
└────────────────────────┼─────────────────────────────────────┘
                         │
                         │ MySQL Queries
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    DATABASE LAYER                            │
│                  (MySQL Database)                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────┐      ┌──────────┐      ┌──────────┐         │
│  │ sanpham  │◄────┤│ danhmuc  │      │donvitinh │◄────┐   │
│  └────┬─────┘     └──────────┘      └──────────┘     │   │
│       │                                               │   │
│       │           ┌──────────┐                        │   │
│       │          ┌┤ donhang  │◄──────┐               │   │
│       │          │└────┬─────┘       │               │   │
│       │          │     │              │               │   │
│       ▼          ▼     ▼              │               │   │
│  ┌───────────────────────────┐  ┌────┴─────┐        │   │
│  │      chitietdn            │  │ nhanvien │        │   │
│  │   (Order Details)         │  └──────────┘        │   │
│  └───────────────────────────┘                      │   │
│                                                      │   │
│  Relationships:                                      │   │
│  • sanpham → danhmuc (Foreign Key)  ────────────────┘   │
│  • sanpham → donvitinh (Foreign Key)  ──────────────────┘
│  • donhang → nhanvien (Foreign Key)                     │
│  • chitietdn → donhang (Foreign Key)                    │
│  • chitietdn → sanpham (Foreign Key)                    │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

## 🔄 Request Flow

### 1. Luồng lấy dữ liệu (GET Request)

```
User                Frontend            API                 Database
 │                     │                 │                     │
 │ 1. Click "Sản phẩm"│                 │                     │
 ├──────────────────>│                 │                     │
 │                     │ 2. Load page    │                     │
 │                     │                 │                     │
 │                     │ 3. GET request  │                     │
 │                     ├────────────────>│                     │
 │                     │                 │ 4. SQL Query       │
 │                     │                 ├───────────────────>│
 │                     │                 │ 5. Return data     │
 │                     │ 6. JSON response│<───────────────────┤
 │                     │<────────────────┤                     │
 │ 7. Display data    │                 │                     │
 │<────────────────────┤                 │                     │
 │                     │                 │                     │
```

### 2. Luồng thêm dữ liệu (POST Request)

```
User                Frontend            API                 Database
 │                     │                 │                     │
 │ 1. Fill form        │                 │                     │
 ├──────────────────>│                 │                     │
 │ 2. Click "Lưu"     │                 │                     │
 ├──────────────────>│                 │                     │
 │                     │ 3. Validate     │                     │
 │                     │                 │                     │
 │                     │ 4. POST request │                     │
 │                     ├────────────────>│                     │
 │                     │                 │ 5. Validate        │
 │                     │                 │                     │
 │                     │                 │ 6. INSERT query    │
 │                     │                 ├───────────────────>│
 │                     │                 │ 7. Return ID       │
 │                     │ 8. Success msg  │<───────────────────┤
 │                     │<────────────────┤                     │
 │ 9. Show toast      │                 │                     │
 │<────────────────────┤                 │                     │
 │ 10. Refresh list   │                 │                     │
 │<────────────────────┤                 │                     │
 │                     │                 │                     │
```

## 🗂️ Cấu trúc Module

### Frontend Modules

```
assets/js/
│
├── api.js ─────────────────────┐
│   ├── API Configuration       │
│   ├── HTTP Request Wrapper    │
│   ├── ProductAPI              ├─► Core API Module
│   ├── CategoryAPI             │   (Used by all pages)
│   ├── EmployeeAPI             │
│   ├── OrderAPI                │
│   ├── StatisticsAPI           │
│   └── Utility Functions       │
│                                │
├── dashboard.js ◄──────────────┘
│   ├── loadOverviewStats()
│   ├── loadTopProducts()
│   └── loadCategoryStats()
│
├── sanpham.js
│   ├── loadProducts()
│   ├── searchProducts()
│   ├── addProduct()
│   ├── editProduct()
│   ├── deleteProduct()
│   └── renderPagination()
│
├── danhmuc.js
│   ├── loadCategories()
│   ├── addCategory()
│   ├── editCategory()
│   └── deleteCategory()
│
├── nhanvien.js
│   ├── loadEmployees()
│   ├── addEmployee()
│   ├── editEmployee()
│   └── deleteEmployee()
│
├── donhang.js
│   ├── loadOrders()
│   ├── createOrder()
│   ├── viewOrderDetail()
│   ├── addOrderDetail()
│   ├── updateOrderTotal()
│   └── deleteOrder()
│
└── thongke.js
    ├── loadRevenueByMonth()
    ├── loadEmployeePerformance()
    └── loadCategoryStats()
```

### Backend Modules

```
api/
│
├── sanpham.php
│   ├── GET    /sanpham.php              → List products
│   ├── GET    /sanpham.php?masp=1       → Get product
│   ├── POST   /sanpham.php              → Create product
│   ├── PUT    /sanpham.php              → Update product
│   └── DELETE /sanpham.php?masp=1       → Delete product
│
├── danhmuc.php
│   ├── GET    /danhmuc.php              → List categories
│   ├── GET    /danhmuc.php?madm=1       → Get category
│   ├── POST   /danhmuc.php              → Create category
│   ├── PUT    /danhmuc.php              → Update category
│   └── DELETE /danhmuc.php?madm=1       → Delete category
│
├── donvitinh.php
│   ├── GET    /donvitinh.php            → List units
│   ├── POST   /donvitinh.php            → Create unit
│   ├── PUT    /donvitinh.php            → Update unit
│   └── DELETE /donvitinh.php?madv=1     → Delete unit
│
├── nhanvien.php
│   ├── GET    /nhanvien.php             → List employees
│   ├── GET    /nhanvien.php?manv=1      → Get employee
│   ├── POST   /nhanvien.php             → Create employee
│   ├── PUT    /nhanvien.php             → Update employee
│   └── DELETE /nhanvien.php?manv=1      → Delete employee
│
├── donhang.php
│   ├── GET    /donhang.php              → List orders
│   ├── GET    /donhang.php?sodh=1       → Get order detail
│   ├── POST   /donhang.php              → Create order
│   ├── PUT    /donhang.php              → Update order
│   └── DELETE /donhang.php?sodh=1       → Delete order
│
└── thongke.php
    ├── GET /thongke.php?type=overview
    ├── GET /thongke.php?type=sanpham_banchay
    ├── GET /thongke.php?type=doanhthu_thang
    ├── GET /thongke.php?type=sanpham_danhmuc
    └── GET /thongke.php?type=nhanvien_hieusuat
```

## 🔐 Security Features

### 1. SQL Injection Prevention
```php
// ✅ Sử dụng Prepared Statements
$stmt = $conn->prepare("SELECT * FROM sanpham WHERE masp = ?");
$stmt->bind_param("i", $masp);
$stmt->execute();
```

### 2. Input Validation
```javascript
// Frontend validation
if (!data.tensp.trim()) {
    showAlert('warning', 'Vui lòng nhập tên sản phẩm');
    return;
}

// Backend validation
if (!isset($input['tensp']) || empty($input['tensp'])) {
    sendError('Tên sản phẩm không được để trống');
}
```

### 3. Error Handling
```php
// ✅ Try-catch blocks
try {
    $conn->begin_transaction();
    // Operations
    $conn->commit();
} catch (Exception $e) {
    $conn->rollback();
    sendError('Lỗi: ' . $e->getMessage());
}
```

### 4. CORS Configuration
```php
// ✅ CORS headers trong database.php
header('Access-Control-Allow-Origin: *');
header('Access-Control-Allow-Methods: GET, POST, PUT, DELETE');
```

## 📊 Database Design

### Entity Relationship Diagram

```
┌─────────────┐         ┌─────────────┐
│  danhmuc    │         │ donvitinh   │
├─────────────┤         ├─────────────┤
│ madm (PK)   │         │ madv (PK)   │
│ tendm       │         │ tendv       │
└──────┬──────┘         └──────┬──────┘
       │                       │
       │ 1                     │ 1
       │                       │
       │        ┌──────────────┴──────────────┐
       │        │                             │
       │        │         sanpham             │
       │        ├─────────────────────────────┤
       └───────►│ masp (PK)                   │
              1 │ tensp                       │
       ┌────────┤ giaban                      │◄────┐
       │      │ giamgia                     │     │
       │      │ hinhsp                      │     │
       │      │ congdung                    │     │
       │      │ xuatxu                      │     │
       │      │ cachdung                    │     │
       │      │ madm (FK) ──────────────────┘     │
       │      │ madv (FK) ────────────────────────┘
       │      └─────────────────────────────┘
       │ N                                    N
       │                                      │
       │      ┌─────────────────────────────┐│
       │      │       chitietdn             ││
       │      ├─────────────────────────────┤│
       │      │ sodh (PK, FK) ───────┐      ││
       └─────►│ masp (PK, FK)        │      ││
            1 │ sl                   │      ││
              │ gia                  │      ││
              └──────────────────────┼──────┘│
                                     │       │
                                   N │       │ N
                                     │       │
              ┌──────────────────────┘       │
              │                              │
              │         donhang              │
              ├──────────────────────────────┤
              │ sodh (PK)                    │
              │ ngaylao                      │
              │ giamgia                      │
              │ manv (FK) ────────┐          │
              └───────────────────┼──────────┘
                                  │
                                  │ N
                                  │
              ┌───────────────────┘
              │
              │      nhanvien
              ├──────────────────────────────┐
              │ manv (PK)                    │
            1 │ hoten                        │
              │ gt (ENUM)                    │
              │ ns (DATE)                    │
              │ ngayvl (DATE)                │
              └──────────────────────────────┘
```

## 🎯 Design Patterns

### 1. MVC-inspired Architecture
- **Model**: PHP API + MySQL Database
- **View**: HTML Pages
- **Controller**: JavaScript files

### 2. RESTful API Design
- Sử dụng HTTP methods chuẩn (GET, POST, PUT, DELETE)
- Resource-based URLs
- JSON responses
- Status codes chuẩn

### 3. Separation of Concerns
- **Presentation Layer**: HTML/CSS
- **Business Logic**: JavaScript
- **Data Access**: PHP APIs
- **Data Storage**: MySQL

### 4. DRY (Don't Repeat Yourself)
- Shared API helper functions
- Reusable UI components
- Common database functions

## 🔧 Technology Stack Details

### Frontend Stack
```
┌─────────────────────────────────────┐
│         HTML5 (Structure)           │
├─────────────────────────────────────┤
│      Bootstrap 5 (UI Framework)     │
├─────────────────────────────────────┤
│      Custom CSS (Styling)           │
├─────────────────────────────────────┤
│   JavaScript ES6+ (Logic)           │
├─────────────────────────────────────┤
│   Bootstrap Icons (Icons)           │
└─────────────────────────────────────┘
```

### Backend Stack
```
┌─────────────────────────────────────┐
│         PHP 7.4+ (Server)           │
├─────────────────────────────────────┤
│         MySQLi (Database)           │
├─────────────────────────────────────┤
│      Apache (Web Server)            │
└─────────────────────────────────────┘
```

## 📈 Performance Optimization

### 1. Database
- ✅ Indexes on primary keys
- ✅ Foreign key constraints
- ✅ Efficient queries with JOINs
- ✅ Pagination to limit results

### 2. Frontend
- ✅ Lazy loading (pages load on demand)
- ✅ Minimal external dependencies
- ✅ Optimized CSS (single file)
- ✅ Event delegation

### 3. API
- ✅ Prepared statements (compiled once)
- ✅ Connection pooling via MySQLi
- ✅ JSON encoding optimized
- ✅ Early returns on errors

## 🧪 Testing Strategy

### 1. API Testing (Postman)
- ✅ 27 pre-configured requests
- ✅ Sample data for POST/PUT
- ✅ Error scenarios covered

### 2. UI Testing (Manual)
- ✅ All CRUD operations
- ✅ Form validations
- ✅ Responsive design
- ✅ Error handling

### 3. Database Testing
- ✅ Foreign key constraints
- ✅ Data integrity
- ✅ Transaction rollback

## 🚀 Deployment Architecture

```
┌──────────────────────────────────────────────┐
│           Production Server                   │
│                                               │
│  ┌────────────────────────────────────────┐  │
│  │         Apache Web Server              │  │
│  │                                         │  │
│  │  ┌──────────────┐  ┌──────────────┐   │  │
│  │  │   Static     │  │   PHP-FPM    │   │  │
│  │  │   Files      │  │   (APIs)     │   │  │
│  │  └──────────────┘  └──────┬───────┘   │  │
│  └────────────────────────────┼───────────┘  │
│                               │               │
│  ┌────────────────────────────┼───────────┐  │
│  │         MySQL Server       ▼           │  │
│  │    ┌────────────────────────────┐      │  │
│  │    │  qlthuocanhangca1 Database │      │  │
│  │    └────────────────────────────┘      │  │
│  └─────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

---

**Architecture designed for:**
- ✅ Scalability
- ✅ Maintainability
- ✅ Security
- ✅ Performance
- ✅ User Experience


